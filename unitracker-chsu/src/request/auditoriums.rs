use crate::model::auditoriums::*;
use crate::request::constants;
use crate::request::constants::*;

// pub async fn get_auditoriums() -> AuditoriumList {
//     let url = BASE_URL.to_owned() + AUDITORIUM;
//     let auditoriums = reqwest::get(url);
// }